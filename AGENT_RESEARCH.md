# 量化 Agent 纵向切片：实现与真实数据验收

日期：2026-09-24
状态：Agent、基础可靠性融合和锁定协议的时间外诊断已实现；完整订单回测与优化仍未实现。

## 已实现内容

- `adaptive_mas.agents.quantitative`：三个确定性专家，输入同一决策日 `MarketSnapshot`，不读取文件、不访问网络、不查看其他 Agent 的观点。
  - `trend`：20 个交易日后复权收盘收益。
  - `reversal`：5 个交易日后复权收盘收益取反。
  - `liquidity_confirmation`：5 日价格收益乘以近期 5 日与此前 20 日平均成交额之比的对数。
- `AgentSignal`：记录 Agent 版本、股票代码、决策日、五日期限、信号分数、输入截止日和证据日期/来源。当前分数只表达横截面排序，暂不伪装成收益率预测或自报置信度。
- `adaptive_mas.reliability.gating`：可靠性分数取此前最多 20 个已成熟窗口的 Rank IC 均值，并截断负技能；至少积累 20 个窗口前使用等权。积累足够后，将正技能权重与等权各混合 50%；若此前所有技能均不为正，则继续等权。
- `adaptive_mas.evaluation.agent_research`：每五个交易日产生一次决策，下一交易日开盘进入，第五个交易日收盘解析五日目标。诊断比较单 Agent、等权融合、自适应融合和沪深 300 同区间基准，并记录成对信号相关、门控轨迹及逐年结果。
- 每只股票、每个 Agent 的原始信号及证据序列化为被 Git 忽略的 `data/processed/agent_signals.jsonl.gz`；实验 JSON 记录其 SHA-256 和记录数。

配置在 [agent_research.toml](configs/agent_research.toml)，运行结果及逐窗口记录在 [agent_research.json](experiments/agent_research.json)。

## 真实数据验收

使用本地 `train.csv` 与 `data/raw/baostock/fetch-20260924T115050Z/`。研究数据覆盖 2024-01-02 至 2026-03-06，共 524 个交易日、181,381 条后复权行情和 112 个历史成分快照。信号决策窗口为 2024-02-05 至 2026-02-13；五日目标相邻窗口不重叠。

| 方法 | 全部 99 窗口平均 Rank IC | 暖启动后 79 窗口平均 Rank IC | 全部窗口 Top-Decile 平均五日毛收益 | 暖启动后 Top-Decile 毛收益 |
|---|---:|---:|---:|---:|
| 趋势 | -0.0220 | -0.0225 | 0.632% | 0.587% |
| 短期反转 | 0.0135 | 0.0255 | 0.618% | 0.669% |
| 量价确认 | -0.0048 | -0.0014 | 0.425% | 0.320% |
| 等权融合 | -0.0105 | -0.0018 | 0.566% | 0.589% |
| 自适应融合 | -0.0081 | 0.0012 | 0.516% | 0.527% |
| 沪深 300 同区间基准 | — | — | 0.354% | 0.313% |

共同股票截面平均 298.44。暖启动后的自适应门控平均权重为：趋势 31.6%、反转 42.5%、量价确认 25.8%；各 Agent 权重随已成熟 Rank IC 变化。全样本的平均成对截面信号 Rank 相关为：趋势/反转 -0.439、趋势/量价确认 0.100、反转/量价确认 -0.035。专家有不同排序信息，但目前不能据此断言它们在组合上有互补价值。

### 结果解释

这是开发区间上的滚动前推诊断。每个决策点的门控仅使用此前已经到期的结果，但专家公式和配置是在这段研究历史中确定的，因此结果不是独立锁定样本外验证。2026 年仅有 6 个评价窗口。

暖启动后，自适应融合 Rank IC 比等权高约 0.0030；Top-Decile 平均五日毛收益却低约 0.062 个百分点。当前结果**没有证明自适应门控优于等权**。短期反转在本轮历史区间的指标相对较好，量价确认贡献较弱；不应根据同一段历史继续反复调公式来追逐更高数值。

Top-Decile 回报是按后复权行情计算的信号排序诊断，没有计入佣金、印花税、冲击成本、涨跌停、停牌、成交容量或实际成交。它不是完整账户回测，也不是可交易性结论。

## 工程验收

- `python -m pytest -q -p no:cacheprovider`：19 项通过。
- `python -m compileall -q src scripts tests`：通过。
- 两次运行的最终结果 JSON SHA-256 完全相同：`152B5E802BD9602AFC4ADEEDEA3B0BA9B55DC55663269478376DE407D522A4A7`；包含 88,946 条逐 Agent 信号的 gzip JSONL SHA-256 为 `76BB71669DB44B3E210E688FE4B1805005223587B9B446D1AF3D1564066446E3`。
- 门控验收轨迹：第一个决策使用 0 个成熟窗口；第 21 个决策开始使用 20 个成熟窗口；99 个窗口中 79 个决策使用自适应权重。
- 生产研究源码中 `test.csv` 引用数为 0；研究 API 仍只读取 `train.csv` 和明确指定的 BaoStock 缓存。
- 运行前后本地源文件 SHA-256 一致：`train.csv` 为 `d5eda5c0e183ecd7b2a9460d75f35dd7c865e0f6a77b46fd59639543c5176bc4`；`test.csv` 为 `3b1807c8ac8f4b4359a5374060f322603e7122f5ff786bb445ad7b9eaabd8389`。

第一轮集成测试的合成指数行漏了 `available_at`，触发 `KeyError`；补齐夹具以符合真实 `ResearchData` 记录后，19 项测试全部通过。真实数据首跑后补充了暖启动后同窗口等权对照、权重范围和成对信号相关报告，再运行两次，结果字节级一致。

## 下一步

锁定公式与门控的时间外结果见 [OOS_RESEARCH.md](OOS_RESEARCH.md)。在 2026-03-16 至 2026-09-23 的 21 个非重叠窗口中，自适应融合没有优于等权；当前专家的 Top-Decile 平均收益在声明的成本情景下均为负。下一步是实现同一成交约定下的简化订单/持仓模拟，尤其是不可成交、停牌延期退出和最低佣金；随后再决定是否保留自适应门控。暂不需要基本面、新闻或宏观数据。
