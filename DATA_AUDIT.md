# Phase 1 数据审计与数据契约

审计日期：2026-09-24（Asia/Shanghai）  
审计数据目录：`C:\Users\xiao\Desktop\THU-BDC2026\data`  
本项目未复制或修改该目录中的 CSV。

## 1. 文件清单

文件大小为实际字节数；行数不含表头。表中日期字段对交易文件指 `日期`，对成分名单指 `updateDate`。

| 文件 | 字节 | 数据行 | 日期范围 | 唯一股票/代码数 | 唯一日期数 | 重复业务键记录 |
|---|---:|---:|---|---:|---:|---:|
| `train.csv` | 17,980,389 | 156,590 | 2024-01-02 至 2026-03-06 | 300 | 524 | 0（`股票代码, 日期`） |
| `test.csv` | 173,185 | 1,500 | 2026-03-09 至 2026-03-13 | 300 | 5 | 0（`股票代码, 日期`） |
| `hs300_stock_list.csv` | 10,448 | 300 | 2026-03-16 至 2026-03-16 | 300 | 1 | 0（`updateDate, code`；重复代码 0） |
| `stock_data.csv` | 19,676,311 | 158,090 | 2024-01-02 至 2026-03-13 | 300 | 529 | 0（`股票代码, 日期`） |

### 精确列名与 Pandas 推断类型

`train.csv`、`test.csv` 和 `stock_data.csv` 的列完全相同：

| 列名 | 推断类型 |
|---|---|
| `股票代码` | `int64` |
| `日期` | `str` |
| `开盘` | `float64` |
| `收盘` | `float64` |
| `最高` | `float64` |
| `最低` | `float64` |
| `成交量` | `float64` |
| `成交额` | `float64` |
| `振幅` | `float64` |
| `涨跌额` | `float64` |
| `换手率` | `float64` |
| `涨跌幅` | `float64` |

`hs300_stock_list.csv` 的精确列名与推断类型为：`updateDate` (`str`)、`code` (`str`)、`code_name` (`str`)。四个文件均可按 UTF-8 读取；`hs300_stock_list.csv` 和 `stock_data.csv` 带 BOM，`train.csv` 和 `test.csv` 不带 BOM。

### 缺失值计数

| 文件 | 缺失列及计数 |
|---|---|
| `train.csv` | `成交量` 231；`成交额` 231；`换手率` 231；`涨跌幅` 231；其余列 0 |
| `test.csv` | `成交量` 2；`成交额` 2；`换手率` 2；`涨跌幅` 2；其余列 0 |
| `hs300_stock_list.csv` | 所有列 0 |
| `stock_data.csv` | `成交量` 233；`成交额` 233；`换手率` 233；`涨跌幅` 233；其余列 0 |

交易文件日期均可解析，没有无效日期。三个日线文件的 `(股票代码, 日期)` 均无重复键。

## 2. 行情字段与文件兼容性

### 代码表示

Pandas 将 `train.csv` 和 `stock_data.csv` 的 `股票代码` 推断为整数，因此直接读取会丢失深圳股票代码的前导零。`train.csv` 原始代码文本长度分布为 1 位 1,048 行、2 位 524 行、3 位 16,768 行、4 位 20,720 行、6 位 117,530 行。`stock_data.csv` 原始代码均为 6 位。将 `train.csv` 的数字代码左侧补齐到 6 位后，两个文件各自的 300 个代码集合都与名单文件 `code` 后缀集合完全相同，也彼此完全相同。新加载器据此保留/规范为六位字符串；不会改写源文件。

### `train.csv` 与 `stock_data.csv` 数值范围

| 字段 | `train.csv` 最小值 | `train.csv` 最大值 | `stock_data.csv` 最小值 | `stock_data.csv` 最大值 |
|---|---:|---:|---:|---:|
| 开盘 | 2.07441437 | 13,587.14599 | 2.07441437 | 13,587.14599 |
| 收盘 | 2.07441437 | 12,434.728372 | 2.07441437 | 12,434.728372 |
| 最高 | 2.08463316 | 13,587.14599 | 2.08463316 | 13,587.14599 |
| 最低 | 2.05397679 | 12,242.84924748 | 2.05397679 | 12,242.84924748 |
| 成交量 | 323,247 | 5,171,252,926 | 323,247 | 5,171,252,926 |
| 成交额 | 26,129,103.54 | 90,037,647,343.97 | 26,129,103.54 | 90,037,647,343.97 |
| 振幅 | 0 | 147.39 | 0 | 147.39 |
| 涨跌额 | -909.49 | 924.78 | -909.49 | 924.78 |
| 换手率 | 0.0224 | 73.0873 | 0.0224 | 73.0873 |
| 涨跌幅 | -20.0177 | 304.3478 | -20.0177 | 304.3478 |

这些数值是 CSV 中的原始尺度，文件没有说明价格、成交量、成交额或百分数字段的单位；本审计不对它们做单位解释或换算。两个文件的 OHLC 均逐行满足 `最低 ≤ 开盘/收盘 ≤ 最高`。

### 重叠记录逐项比较

按六位代码和日期匹配后，两文件在 524 个共同交易日上有 156,590 个共同 `(symbol, trade_date)` 键，覆盖 300 个代码。逐项比较 `open, close, high, low, volume, amount, turnover, return_1d`：

- OHLC 每项 156,590 个值全部相等，最大绝对差为 0。
- 成交量、成交额、换手率、涨跌幅各有 156,359 个非空值，全部相等，最大绝对差为 0；其余 231 个值的缺失位置一致。
- 因此，两文件重叠训练区间的 156,590 行数据逐项一致。未合并文件。

**训练源选择：`train.csv`。** 它明确限定在训练区间，且与 `stock_data.csv` 的全部重叠记录完全相同。`stock_data.csv` 比训练数据多 1,500 行，日期为 2026-03-09 至 2026-03-13，正好是 `test.csv` 的日期区间，因此不能作为研究加载入口。生产加载器固定打开指定目录下的 `train.csv`。

数值一致只能证明文件间记录相同，不能证明来源的价格定义或复权方式。文件未包含复权标记、调整因子或足以判定复权方式的来源元数据；未进行外部行情抽查。价格调整语义仍未确定。

## 3. 沪深 300 成分名单

`hs300_stock_list.csv` 的 `updateDate` 只有一个值：`2026-03-16`，该快照包含 300 个不同代码。`updateDate, code` 无重复，单日代码重复数为 0；格式样例为 `sh.600000`、`sh.600009`，股票代码部分为六位数字。

分类：**B，仅一个当前快照，不是历史成分快照。** 将其当作 2024 年以来的历史成分会造成幸存者偏差。即使行情代码集合与此快照吻合，也不能证明这些股票在整个历史区间都是沪深 300 成分股。

## 4. `test.csv` 隔离审计

`test.csv` 与 `train.csv` 有相同的 12 列和相同的推断类型。它包含 300 个同一代码集合、5 个日期、每日期 300 行；日期为 2026-03-09 至 2026-03-13，与 `train.csv` 的日期不重叠，并晚于训练数据最后日期 2026-03-06。其四个缺失数值列各有 2 个缺失值。

研究数据 API 只拼接固定文件名 `train.csv`，没有 `test.csv` 加载代码。测试还静态检查 `src/adaptive_mas` 下生产 Python 源码不引用 `test.csv`，并以无效的同目录 `test.csv` 验证加载器只读训练文件。`test.csv` 的本次审计读取仅用于报告上述结构与日期关系；不得把它用于特征、训练、归一化、可靠性、状态识别、组合构建或调参。

## 5. 最小规范行情数据契约

| 规范字段 | 来源列 | 类型 | 处理 |
|---|---|---|---|
| `symbol` | `股票代码` | 六位字符串 | ASCII 数字左补零到六位；不转整数 |
| `trade_date` | `日期` | `date` | ISO 日期解析；源文件没有日内时间 |
| `open` | `开盘` | 数值 | 保留源数值尺度 |
| `high` | `最高` | 数值 | 保留源数值尺度 |
| `low` | `最低` | 数值 | 保留源数值尺度 |
| `close` | `收盘` | 数值 | 保留源数值尺度 |
| `volume` | `成交量` | 数值或空值 | 保留源值；不推断单位 |
| `amount` | `成交额` | 数值或空值 | 保留源值；不推断币种/单位 |
| `amplitude` | `振幅` | 数值或空值 | 保留源值；不按百分比换算 |
| `price_change` | `涨跌额` | 数值或空值 | 保留源值 |
| `turnover` | `换手率` | 数值或空值 | 保留源值；不按百分比换算 |
| `return_1d` | `涨跌幅` | 数值或空值 | 保留源值；不按百分比换算 |

边界检查：必需列存在且表头不重复；代码和日期非空并可解析；数值可解析且有限；OHLC 关系有效；规范化后的 `(symbol, trade_date)` 唯一。已观测到的次要指标空值会保留为 `None`，不补零。所有错误直接报出，不使用备用文件或静默默认值。

### 点时定义

- `event_time`：该日行情对应的交易日，取 `trade_date`。它是日期标签，不代表某个被数据证明的日内时刻。
- `available_at`：数据源将完整日线发布/交付给研究系统的带时区时间。四个本地文件都没有此字段，因此当前记录的 `available_at` 未知，加载器不生成时间戳。
- 点时约束仍为 `available_at <= decision_time`。日线 OHLC 只有在交易时段结束且最终日线已发布后才可用。当前文件无法证明当日收盘后具体发布时刻；在获得发布时刻证据前，不允许同一交易日内的决策使用该日完整日线。历史决策只能在后续交易日使用已完成且可证明已发布的记录。不能用虚构的 15:00 或次日开盘时间填补 `available_at`。

### 后续数据记录的建议字段（本阶段只定义，不实现）

所有时间戳用带时区时间；每条记录保留来源、来源记录标识、抓取时间和原始响应引用。

| 记录 | 建议字段 |
|---|---|
| `FundamentalRecord` | `symbol, metric, fiscal_period, value, unit, published_at, available_at, source, source_record_id, fetched_at, raw_response_ref, revision_id` |
| `AnnouncementRecord` | `announcement_id, symbol, announcement_type, published_at, available_at, source, source_record_id, fetched_at, raw_response_ref` |
| `MacroRecord` | `series_id, observation_period, value, unit, released_at, available_at, source, source_record_id, fetched_at, raw_response_ref, revision_id` |
| `IndexMembershipRecord` | `index_id, symbol, effective_from, effective_to, announced_at, available_at, source, source_record_id, fetched_at, raw_response_ref` |

## 6. 最小项目骨架与 Phase 1 验收

新仓库位于 `adaptive-mas/`，未复制旧项目源码。只创建了数据加载所需的两个 `__init__.py`；未来模块目录没有占位类或空 API。当前只有 `src/adaptive_mas/data/market_data.py` 实现列映射、代码规范化、日期解析、训练文件读取和外部数据边界校验。

Phase 1 验收点：

1. 实际四个 CSV 清单、字段、类型、日期、代码、缺失和重复已记录。
2. `train.csv` 与 `stock_data.csv` 兼容性按共同键和 8 个字段实测。
3. 选择 `train.csv` 为唯一训练行情源；`stock_data.csv` 的 5 个评分日期不进入 API。
4. 成分名单判定为单一快照，并记录幸存者偏差风险。
5. 复权语义明确标记为未确定。
6. `test.csv` 的结构与隔离已记录并由生产代码静态扫描测试约束。
7. 最小目录骨架、文档和数据边界已创建。
8. 加载器验证了必需列、代码、日期、有限数值、OHLC 和重复键。
9. 聚焦测试覆盖代码、格式错误、重复键、OHLC、源文件只读和评分文件隔离。
10. 没有实现 Agent、模型、目标、特征、优化或回测。

验证结果：pytest 统一入口收集六项边界测试并全部通过；项目 wheel 可用本机现有构建工具构建，安装后的加载器读取完整训练文件共 156,590 行、300 个代码、524 个交易日。独立规范见 [DATA_CONTRACT.md](DATA_CONTRACT.md)，未来模块边界见 [ARCHITECTURE.md](ARCHITECTURE.md)。实施后四个原始 CSV 的 SHA-256 与审计开始时记录完全一致。

## 7. Phase 2 数据源建议

只补齐当前架构必需的数据：

1. **历史沪深 300 成分及其行情**：采用用户指定的 BaoStock `query_hs300_stocks(date=...)`，本项目已实际取到 2024 年历史快照；同一接口按日期返回 `updateDate, code, code_name`。对历史成分并集读取 BaoStock `query_history_k_data_plus` 日线，包含后来退出当前快照的代码。原本地单快照不再被用作历史池。
2. **价格与指数行情**：本地训练行情由用户确认来自 BaoStock，且 2024-01-02 浦发银行样本与 BaoStock `adjustflag=1` 后复权 OHLC 精确相同；保留其后复权口径作为训练输入。另从相同 BaoStock 来源读取 `adjustflag=3` 原始日线用于执行模拟，分开存放；指数基准使用 BaoStock `sh.000300` 日线。具体采集接口和样本见 [M1_SOURCE_REVIEW.md](M1_SOURCE_REVIEW.md)。

原始 BaoStock 响应缓存在 `data/raw/baostock/`，附请求参数、抓取时间和哈希；单一来源请求失败时直接报错。当前架构并未规定基本面、宏观、新闻或外部风险因子，因此这些数据此时不属于必需项。

## 8. 原始文件完整性

审计开始时记录、实施后再次核对的 SHA-256：

| 文件 | SHA-256 |
|---|---|
| `train.csv` | `d5eda5c0e183ecd7b2a9460d75f35dd7c865e0f6a77b46fd59639543c5176bc4` |
| `test.csv` | `3b1807c8ac8f4b4359a5374060f322603e7122f5ff786bb445ad7b9eaabd8389` |
| `hs300_stock_list.csv` | `814c6e4a4e8090e060e012da7ef554e6caa863c153335b09dc3446b56f1baaa0` |
| `stock_data.csv` | `4cb643c916244b0b6bf732e7501dc5fd416493e5bf49d579fdb5747523ae3ed6` |

## 9. BaoStock 定点样本补充（Phase 1 审计后）

Phase 1 当时因 CSV 未附来源元数据，将复权口径标作未确定。用户随后确认本地数据来自 BaoStock；对浦发银行 2024-01-02 单点读取后，`adjustflag=1` 后复权 OHLC 与 `train.csv` 精确一致，交易量、成交额、换手率、涨跌幅也一致。本项目据此按 BaoStock 后复权训练输入处理，不扩大成全表复核。执行模拟另取 `adjustflag=3` 未复权行情，保持独立数据文件，不拼接或覆盖源 CSV。
