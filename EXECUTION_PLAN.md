# 风险感知自适应多智能体沪深300投资决策系统：执行计划

## 1. 项目边界与已核实的起点

本计划以 `C:\Users\xiao\Desktop\RAAMA\adaptive-mas` 为新项目目录。只允许沿用 `C:\Users\xiao\Desktop\THU-BDC2026\data` 中的原始数据文件；不沿用旧选股项目的代码、特征、模型或架构。每一阶段只实现该阶段所需内容，验收后再进入下一阶段。研究过程和最终报告保留失败与无显著改进的结果。

Phase 1 的现有审计见 [DATA_AUDIT.md](DATA_AUDIT.md)。已经核实：`train.csv` 有 156,590 行、300 只股票、524 个交易日（2024-01-02 至 2026-03-06）；`stock_data.csv` 与训练集重合的 156,590 个股票日期观测在八个比较字段上完全一致，另有 2026-03-09 至 2026-03-13 的 1,500 行；`test.csv` 正是这 1,500 行所对应的五个日期；`hs300_stock_list.csv` 只有 2026-03-16 一个 300 只股票的快照。价格复权口径和历史可获得时间仍未证实。当前训练数据入口只读取 `train.csv`，六个边界测试已通过，原始 CSV 未被修改。

以上结论描述 Phase 1 的本地 CSV。随后 P2 已从 BaoStock 单源取得 112 个历史周度成分快照及其 348 只成员的历史行情，因此当前 P3–P8 原型不再用 2026-03-16 单日名单回填整个历史期。周度快照与源公布时点的差异仍按 P2 文档的可用时间约定处理，不能声称精确复原交易所公告时间。`test.csv` 只允许在未来最终本地评分环节由隔离的评分组件读取；研究数据层、特征、标签、训练、调参、可靠性、状态识别、组合构建都不得读取它。

## 2. 核心研究问题与预注册比较

核心问题：在相同的可用信息、预测期限、交易执行、成本和风险约束下，**只用已到期结果更新的智能体可靠性**，以及在有足够样本时引入的**市场状态条件可靠性**，能否比单一信号、等权融合和静态融合改善样本外扣除交易成本后的组合表现，并使预测不确定性与实际误差更一致？此为待验证假设，不把改进预设为结论。

主要比较链：市场基准/简单组合 → 单一量化信号 → 多信号等权融合 → 静态权重融合 → 延迟更新的可靠性融合 → 状态条件可靠性融合 → 同一融合结果进入风险约束优化。每次比较保持股票池、数据可用时间、目标、换手和交易成本模型一致。LLM、辩论和强化学习随后各自通过增量实验决定是否保留。

开始建模前冻结一份研究协议：决策时点、最早可执行时点、五个**交易**日的收益定义、停牌及涨跌停处理、现金和交易成本、主评价指标、验证窗口、超参数搜索范围、统计比较方法。若需与竞赛评分对齐，先只读核对评分定义；不得根据 `test.csv` 反推协议。当前建议研究协议为收盘后决策、下一可交易时点执行、持有五个交易日，但必须先证明对应行情与实际公布时间支持这一约定；不得把日线数据解释成未经证实的盘中可交易报价。

## 3. 统一的数据与决策契约

每条研究记录至少携带 `event_time`、`available_at`、来源标识和原始记录定位。只有 `available_at <= decision_time` 的信息可见。每日行情的 `event_time` 是交易日，具体 `available_at` 应由来源的公布/入库规则与审计证据确定；证据不足时，只允许保守的次日决策约定，并把这一假设写入协议。抓取时间 `fetched_at` 不等于历史公布时间。原始响应不可变，派生数据可重建。

五个交易日目标在 Phase 3 才实现。智能体输出同一预测期限的预期收益、估计不确定性、数据截止时点和可追踪依据；概率与置信度由独立校准环节定义，避免不同智能体自报的置信度直接相加。融合器只处理标准化数值输出；优化器基于收益、风险、流动性和约束形成权重，并记录从输入到交易决定的完整证据链。

回测的历史股票池必须按当时已生效且当时已知的成分信息构造。当前单日成分快照仅可用于标明为“固定快照股票池”的原型实验。

## 4. 分阶段实施与验收

| 阶段 | 本阶段只做什么 | 可检查的交付物与退出条件 |
| --- | --- | --- |
| P1 收尾：数据审计与契约 | **已完成。** 保留审计与训练集边界；新增独立 `DATA_CONTRACT.md` 和只描述未来决策链/职责的 `ARCHITECTURE.md`；统一 pytest 入口，不增加未来模块实现。 | 项目 wheel 在本机现有构建工具下成功构建并安装到项目内临时位置；pytest 6 项通过；安装后的加载器读取真实训练文件为 156,590 行、300 个代码、524 个日期；四个源 CSV 的 SHA-256 与审计记录一致。 |
| P2：时间点一致的数据底座 | **已实现基础版本。** BaoStock 原始行情与成员已缓存；`ResearchData.snapshot_at` 统一提供按日期可见的数据，并分离信号行情与执行行情。 | 真实数据起止日重放成功；决策日之后的行情不可见；未来成分快照要等 `updateDate` 后首个交易日才可见；`test.csv` 不进入生产加载 API。成员公告时间未由 BaoStock 返回，文档记录这一限制，不声称已重建公告时点。 |
| P3：最小特征与五日目标 | **首个 MVP 已实现。** 三个收盘后可见量价专家信号和成熟标签驱动的 Ridge 基线；严格按时间前推，训练标准化只使用成熟历史目标。 | 99 个非重叠评价窗口；决策记录、标签截止时间、输入截止日可重放；未读取 `test.csv`。实现边界与样本外诊断见 [MVP_RESEARCH.md](MVP_RESEARCH.md)。 |
| P4：回测器与量化基线 | **最小日线账户模拟已实现。** 所有策略共用下一日开盘名义成交、停牌状态、历史成交额参与率上限和费用。 | 同协议比较指数、单专家、融合与 Ridge；落盘目标、订单、成交/阻塞、持仓、净值和指标。日线模拟不是逐股/券商级回放，限制已写入实验报告。 |
| P5：量化专家智能体 | **首批三个数值专家已实现。** 趋势、短期反转、成交额扩张确认共享同一时点股票池与输出期限，不自行下单。 | 与等权、静态权重、自适应融合及 Ridge 同窗比较；角色是否有增益按真实数据报告，不预设多 Agent 有效。 |
| P6：可靠性与自适应融合 | **基础 Rank IC 可靠性已实现。** 只使用成熟标签，比较等权、开发期静态与滚动自适应权重。 | 有逐决策权重和成熟窗口日志。未实现概率校准；开发区间自适应融合未超过等权，完整数字见 [MVP_RESEARCH.md](MVP_RESEARCH.md)。 |
| P7：市场状态条件权重 | **两态原型已实现。** 使用沪深300过去 20 日价格涨跌分类，单状态样本不足时回退到全局成熟可靠性。 | 开发及锁定协议 OOS 均已评价；OOS 只有 21 个窗口且区间很宽，尚不足以证明条件权重增益。 |
| P8：风险约束组合层与首个研究 MVP | **最小组合分配已实现。** 有 Top-K、95% 投资目标、单股上限、现金、换手、成交额参与率、费用、持仓与净值轨迹；另有逆波动率风险分配。 | P3–P8 流程在真实 BaoStock 缓存上重放并通过 30 项测试。当前不是完整风险优化器：无协方差/CVaR、真实股数、涨跌停排队、真实冲击与券商回报。阶段不等同项目整体完成。 |
| P9：基本面与公告型 LLM 专家 | 此时才获取必要的财报、公告及其正式发布时间，构造时间点一致的结构化输入；LLM 用于提炼可核查证据并输出统一契约。 | 每条结论能追溯原文和当时可见时间；对比无 LLM 的 P8，单独报告增量、成本、稳定性及错误案例。 |
| P10：新闻与宏观专家 | 仅在 P9 的增量实验表明有必要拓展异质信息后，获取带发布时间、来源与修订信息的新闻/宏观数据。 | 无时间穿越；增量实验优于恰当的简单替代方案，否则不纳入主系统。 |
| P11：辩论与记忆 | 在结构化证据和已验证专家之上研究分歧处理；记忆只保存截至决策时点已知的事件与已成熟结果。 | 辩论/记忆与无辩论/无记忆相比的独立贡献可识别；成本、延迟及错误传播被记录。 |
| P12：强化学习与最终评价 | 仅在前述基线稳定后，研究 RL 是否在交易/权重控制上产生额外价值；随后冻结全系统，用锁定的样本外数据作一次最终评价。 | 逐步消融、成本敏感性、不同市场区间、统计不确定性、全部配置记录与失败结果齐全；`test.csv` 只经隔离的本地评分器访问，绝不参与选择或重训。 |

P2 只交付行情与成分的时间点底座。附件方案中提到的 `fundamentals` 数据接口推迟至 P9，避免在尚无基本面专家时提前获取和实现；P2 仍须在数据契约中预留 `FundamentalRecord`、`AnnouncementRecord`、`MacroRecord`、`IndexMembershipRecord` 的来源与 `available_at` 语义。没有阶段需要的功能，不创建空实现。

## 5. 最小外部数据需求与获取时点

| 何时需要 | 数据 | 用途和验收重点 | 候选来源 |
| --- | --- | --- | --- |
| P2 启动前 | 沪深300历史成分及调整快照 | 使用日期参数取得历史成员，覆盖后来退出当前快照的代码。 | BaoStock `query_hs300_stocks(date=...)`。 |
| P2 同时 | 历史成分股日行情及执行价格 | 后复权本地训练输入与未复权执行价格分开保留。 | BaoStock `query_history_k_data_plus`，训练源按 `adjustflag=1`，执行源按 `adjustflag=3`。 |
| P4 前 | 沪深300指数日行情 | 与股票历史同步的市场基准序列。 | BaoStock `sh.000300` 指数日线。 |
| P9 前 | 财报、财务指标、正式公告及披露时间 | 支撑真正异质的基本面/事件专家；仅在相应实验获准开始时获取。 | 交易所及上市公司正式披露、具有完整时间戳与授权的文档化供应商。 |
| P10 前，且仅在需要时 | 新闻、宏观数据及其发布时间/修订版本 | 检验是否有价格之外的增量信息。 | 官方发布机构或具备发布时间、历史版本和使用许可的供应商。 |

Phase 1 不下载外部数据；进入 P2 后，按本计划已限定的必要数据从 BaoStock 获取历史成员、执行用未复权日线和指数基准。不会添加备用供应商或与已有训练文件重复下载后复核。

## 6. 验证方法与结果判定

使用按日期滚动的训练/验证/测试窗口，不随机拆分同一交易日的股票。五日目标在窗口边界实行至少覆盖目标重叠长度的隔离，并使特征、归一化、可靠性、状态和超参数仅用当时已知数据。训练集内部多次滚动验证；最终保留的时间段仅在系统冻结后评价一次。由于本地训练集只有 524 个交易日，报告窗口数和时间块不确定区间，不把 300 只股票 × 524 日当作独立样本量。

预先指定一个主要组合指标和一组硬风险条件。辅助指标包括五日预测的 IC/rank IC、误差与校准，组合净收益、Sharpe/Sortino、最大回撤、CVaR、换手、成交限制与交易成本。每次实验保留完整配置、数据清单、代码版本、训练截止时点与所有尝试记录；对相同交易日窗口做成对比较。若自适应系统未在样本外稳定优于等权或静态融合，则明确报告无增益，并回到更简单方案。

可借鉴 [Qlib 的数据与策略接口](https://github.com/microsoft/qlib)、[TradingAgents 的角色分工](https://github.com/TauricResearch/TradingAgents)、[FinCON 的多智能体讨论设定](https://arxiv.org/abs/2407.06567)，但不复制其实现或假设它们在本市场有效。等权融合作为强基线有[预测组合研究](https://www.sciencedirect.com/science/article/pii/S0169207016000327)支持；概率校准方法可参照[校准研究](https://proceedings.mlr.press/v70/guo17a.html)，其在本任务的有效性仍需实测；CVaR 形式可参照[原始论文](https://sites.math.washington.edu/~rtr/papers/rtr179-CVaR1.pdf)。

## 7. 从架构到代码和实验的下一步计划

实施原则：架构采用 [ARCHITECTURE.md](ARCHITECTURE.md) 中的模块职责和数据契约；每次只实现当前里程碑需要的模块。实验若不支持自适应多 Agent 优于简单基线，就保留简单方案并如实报告，不把“Agent 数量”当成功标准。

### M1：锁定时间点数据源和研究协议

**进度（2026-09-24）：**BaoStock 单源接入及必要行情采集已完成，记录于 [M1_SOURCE_REVIEW.md](M1_SOURCE_REVIEW.md)。本地 `train.csv` 的 300 个代码都出现在历史成分并集中；另外 48 个历史成员已从 BaoStock 单独取得 `adjustflag=1` 行情。原始数据还包括全部 348 个成员代码的 `adjustflag=3` 行情、112 个周度成分快照和沪深300指数。数据边界测试共 13 项通过。决策可用时间以次交易日日期粒度约定，不声称源数据含历史发布时间。交易费用、未成交处理、基准回报口径和滚动窗口在首组基线实验前冻结。

使用 BaoStock 单一接口读取历史成分、执行用未复权日线和沪深300指数日线。采集范围由 `train.csv` 日期确定，周度保存成员快照，并为历史成员并集拉取日线。300 个本地训练代码按已对上的 BaoStock 后复权口径使用；另 48 个未覆盖历史成员已补后复权日线。当前 P2 所需原始数据已齐备，剩余工作是实现读取层与时点规则，不再扩充数据类型。

1. 沪深300历史成分快照及其成员并集行情：已按周获取日期参数快照，覆盖 348 个代码；历史公告发布时间未提供。
2. 信号用后复权日线和执行用未复权日线：300 个代码来自本地训练行情，另 48 个历史成员已补后复权序列；348 个代码均有未复权行情及成交状态字段。
3. 沪深300指数日线：已取得，用于 P4 基准收益。

先实现数据采集和可重放的数据边界。日线信号只在当日结束后生成，最早下一交易日执行，不虚构日内时间。五日目标、费用/滑点、无法成交规则、基准回报类型、滚动窗口和主指标在 P3/P4 首个基线开始前冻结；截至 2026-03-06 的现有训练历史用于首轮原型。

**退出条件：**唯一数据源可实际读取；成员快照、训练行情、执行行情和基准按不同用途保留；原始响应及获取参数可重现；采集范围不跨入评分日期；首个实验前研究协议冻结。

### M2：实现 PIT 数据脊柱（P2）

**进度（2026-09-24）：**已实现 `load_research_data(...).snapshot_at(decision_date)`，加载 524 个交易日、181,381 条后复权信号行情、181,381 条独立未复权执行行情、112 个历史成分快照及指数。成员 `available_at` 取源 `updateDate` 后第一个沪深300交易日；日线在收盘后的日期粒度可见，最早下一交易日执行。真实数据起止日重放成功，当前 14 项测试通过。

按职责在 `data/` 中逐步实现规范记录、显式 Provider、不可变原始响应清单、按时点的数据读取和历史 Universe。跨模块类型只在真实消费者出现时建立 `domain.py`。查询统一接收 `decision_time`，只返回 `available_at <= decision_time` 的行情/成分；不允许 Agent 直接打开 CSV 或调用数据供应商。

**验收：**输入未来交易日行情/未来生效快照时，历史查询明确不可见；给定日期能复现最近已可用股票池；原始响应来源、抓取时间、源更新日期与本地规则可追溯；Provider 明确配置且网络失败直接报错；`test.csv` 在生产研究源码和加载 API 中不可访问。BaoStock 未返回成员公告时间，故不把公告时点列作已实现能力。

### M3–M6：P3–P8 研究 MVP（已完成首个版本）

截至 2026-09-25，P3 至 P8 首个最小版本已在开发期和缓存的时间外 BaoStock 区间完成重放。参数、标签成熟规则、成本、执行代理和统计方法被冻结并写入 `configs/mvp_research.toml` 及 [MVP_RESEARCH.md](MVP_RESEARCH.md)。其中 99 个开发标签窗口有 20 个作可靠性暖启动，79 个进入开发账户比较；开发期策略收益区块 Bootstrap 区间均覆盖零。自适应融合未胜过等权。锁定协议时间外窗口共 21 个，状态条件方案本期最好，但不确定区间极宽，不能作为有效性证明。没有根据这些 OOS 结果调参。

当前 P8 明确是 MVP 风险分配与日线名义账户模拟：逆波动率是风险缩放代理，不是均值方差/协方差求解器或 CVaR；执行记录为金额级订单和持仓值，不包含真实股数、最小佣金、涨跌停队列、逐笔冲击和实际分红现金。30 项自动化测试通过；开发输出、OOS 输出、训练种子及两份决策日志重放哈希保持一致。完整结果与口径见 [MVP_RESEARCH.md](MVP_RESEARCH.md)。

因此本阶段可交付的是**可复现研究原型**，而不是完整交易系统或盈利承诺。下一阶段先针对明确的模型失败点做误差和成本归因及稳健基线比较；只有结果能证明必要时，才引入更重的优化方法或新数据源。当前 P3–P8 不需要新增数据集。

**按优化路线图的 M1 补强（2026-09-25）：**在不覆盖 v1 结果的前提下，新增 `fixed_horizon_v2`：次日开盘入场、第五日收盘预登记退出；停牌持仓延期并按实际存量扣减后续买单；预测决策与成熟标签结算单独存账。开发期 99 个决策记录/29,546 条有效预测结算，时间外诊断 21 个决策记录/6,287 条有效结算；M1 结果和协议见 [M1_FIXED_HORIZON_RESEARCH.md](M1_FIXED_HORIZON_RESEARCH.md)。合成未来行情改变测试确认历史分数/目标不变，结算随未来行情变化；账本现金与持仓对账测试通过。该改进没有新增数据，但名义金额估值仍不等于真实股数和公司行动账本。

### M7：可选异构 Agent 与最终样本外验证（P9–P12）

P9 才接入财报/公告文本；P10 才考虑新闻/宏观；P11 的辩论/记忆、P12 的 RL 都须在前一阶段有稳定基线后单独消融。LLM 只能读取经过点时过滤的证据，记录模型、提示、原始响应与时间；记忆只能在结果到期后更新。每个新组件必须证明相对 P8 有样本外增益，不能因架构完整而默认纳入。

最终锁定策略、代码、配置和评估日期后才调用隔离的本地评分器读取 `test.csv`；该结果不得反过来选择模型、参数、门控规则或架构。

### 代码协作与 Git 同步

本地仓库在 `main`，`origin` 已设为 [GitHub RAAMA-v0](https://github.com/NyviaXiao/RAAMA-v0.git)。本次样本外实现和报告已通过独立分支审查、快进整合，并于提交 `ba818c5` 推送至 `origin/main`。用户提供的未跟踪 `base.md` 未纳入提交。
