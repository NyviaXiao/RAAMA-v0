# Adaptive-MAS：优化方向与后续计划

日期：2026-09-25。本文是基于现有实现的研究与开发建议，不表示下述能力已经实现。

## 实施状态更新（2026-09-25）

已按本路线图优先完成 M1 首版，并保留原连续持仓 v1 结果：新增五日计划退出执行、预测决策账/成熟结算账分离、停牌跨期持仓和现金状态纳入后续订单。开发期与时间外诊断及限制见 [M1_FIXED_HORIZON_RESEARCH.md](M1_FIXED_HORIZON_RESEARCH.md)。M1 账本仍按名义金额近似估值，不代表真实股数或公司行动结算。项目测试现为 35 项通过，原 v1 数据报告没有覆盖。下一步按本路线图进入 M2 收益校准与损失门控比较；当前尚未开始 M2。

## 1. 项目定位与本轮结论

保留 `base.md` 的核心：独立专家产出信号，成熟结果更新可靠性，门控分配专家权重，数学优化器分配资产权重，成交结果回流。最值得研究的问题是：**在相同信息、执行和风险预算下，可靠性驱动的专家融合是否提高成本后的投资效用。**

“最优化”分成两层：给定预测、成本与约束，求解明确的组合优化问题；通过时间外实验检验预测和融合是否有用。优化器的数学最优解不等于真实市场中的最优策略。

下一阶段直接补齐现有 Multi-Agent 纵向闭环。已有量化专家继续作为系统主体，数据工作只补当前成交与记账需要的字段，不重新启动全表审计或多数据商对比。

本路线图形成时读取了 `base.md`、当时的架构/执行计划/研究报告、主要源码与实验 JSON，核对官方开源项目资料，并使用项目 `.venv` 重跑测试：22 passed。系统默认 Python 未安装本项目及 BaoStock，正确的复现环境是项目已有虚拟环境。该记录描述路线图形成时的工作区状态；后续 M1 实现见上面的状态更新。

**路线图形成时的工作区快照：** 当时工作区已出现 `backtest/simulator.py`、`portfolio/allocation.py`、`regime/market_state.py`、`models/ridge.py` 和 `configs/mvp_research.toml`，并有 NumPy 依赖。代码包含按名义金额记账的日频模拟、Top 组合/逆波动配置、二元状态条件权重和 Ridge 基线。下文最初的“尚缺”分析针对当时已验证的 v1；后续实施状态以本文开头的更新及 M1 报告为准。

新代码与本路线图的关键接点是：名义金额/后复权回报模拟应明确与真实股数账本的区别；逆波动配置应作为风险基线，与收益—风险—成本联合优化分开比较；固定 95% 投资比例是在检验选股，不是在检验现金择时；状态样本不足时回到全局权重是显式算法规则，可与连续收缩比较。现有持续持仓模拟还需与预测期限、实际持仓漂移和未成交后的下一次目标生成对齐。

## 2. 已有资产与尚未完成的闭环

| 能力 | 已有证据 | 下一步用途 |
| --- | --- | --- |
| 时点行情与股票池 | BaoStock 单源；后复权信号价与未复权执行价分离；历史成分快照 | 直接沿用，保留日粒度可用时间和周度成分快照的既有限制 |
| 开发数据 | 2024-01-02 至 2026-03-06，524 个交易日、181,381 条后复权记录 | 滚动训练、校准及开发期比较 |
| 时间外数据 | 2026-03-16 至 2026-09-23，133 个交易日；后复权与未复权各 42,427 条 | 原版本历史时间外结果；新版本可用于诊断，不能反复使用后仍称最终未见测试 |
| 专家 | 20 日趋势、5 日反转、量价确认；独立读取同一个 MarketSnapshot | 冻结 v1 为可重放基线 |
| 信号契约 | score、期限、截止日、版本、证据引用 | 在其后增加收益校准，不把 score 改名冒充收益率 |
| 可靠性 | 最近 20 个成熟窗口 Rank IC；50% 等权混合 | 保留为旧门控对照 |
| 评价 | 单专家、等权融合、自适应融合；99 个开发窗口、21 个时间外窗口 | 接入同一账户模拟器后重新比较 |
| 尚缺能力 | 收益校准、独立订单与持仓账本、风险优化、状态条件门控、文本 Agent | 按下文四个里程碑补齐 |

现有时间外结果是五日窗口简单均值：

| 方法 | Rank IC | Top 10% 毛收益 | 情景成本后收益 |
| --- | ---: | ---: | ---: |
| 趋势 | -0.0575 | -0.372% | -0.583% |
| 反转 | 0.0162 | -0.080% | -0.292% |
| 量价确认 | -0.0162 | 0.087% | -0.125% |
| 等权融合 | -0.0223 | -0.143% | -0.355% |
| 自适应融合 | -0.0314 | -0.370% | -0.581% |

来源：`experiments/oos_agent_research.json`、`OOS_RESEARCH.md`。这些数值不是年化收益或账户净值。自适应相对等权平均每窗口约低 22.7 bp；当前证据不支持 H2。只有 21 个时间窗口，不能把约 300 只股票乘以窗口数当作独立市场样本量。

## 3. 六个优先优化方向

### A. 在决策时形成真实的投资组合

`evaluation/agent_research.py::_evaluate_pending` 先取未来标签存在的股票，再计算共同截面的百分位、融合和 Top 10%。对于注明覆盖范围的信号诊断可以解释，但不能直接作为可交易账户的选股过程：未来某股票是否有出场价，不应改变今天的选股名单或其他股票的归一化。

应将当日股票池、标准化分数、融合结果、目标权重和订单在决策时写定。事后只结算标签和成交，不能因未来标签缺失重排股票或补选更低排名股票。未买入资金留在现金，不能卖出的持仓继续保留并按既定规则估值。

同时分开两本账：

- 预测账：所有有效预测按固定期限结算，用于校准与可靠性；记录无标签覆盖情况。
- 投资账：实际订单、成交、现金、持仓、公司行动与成本，用于组合绩效。

没有买到某股票，不代表它的价格预测错误；卖出受阻造成的额外持有收益，也不能冒充固定五日预测标签。

### B. 从排序分数走向收益与误差估计

当前三个 Agent 是不同的确定性量化专家；这一形式可以保留。需要补的是标准化后的分数与未来收益之间的关系。

第一版建议用训练折内、已成熟的预测记录拟合简单收缩线性映射：`mu_i = intercept_i + slope_i * percentile_score_i`，以分桶收益曲线作可视化诊断。比较校准前后的排名、收益误差与稳定性。拟合损失按日期组织，避免把同一天大量相关股票当作大量独立证据。

区分预测残差波动与预期收益估计的不确定性。日常价格波动属于风险模型，均值估计不稳定属于校准；不能把同一个波动率在风险项和不确定性项重复惩罚。

若以后使用 `probability_up`，先定义事件，例如固定持有期限绝对收益为正，再拟合概率并评价 Brier/校准曲线。当前阶段不要求每个 Agent 填写没有统计依据的 confidence。

**必须明确绝对收益与超额收益。** 横截面强弱不能单独决定股票与现金的配置。如果校准目标是相对指数收益，要加入独立的市场收益假设/模型后才构成绝对收益预测；或者明确做固定风险预算的选股实验。正超额预测不能解释为一定值得增加总股票仓位。

### C. 让门控学习可比较的损失

当前门控有三个可检验的限制：

1. 最近 20 个五日窗口约相当于 100 个交易日，状态变化时可能反应较慢。
2. 以 Rank IC 更新权重，却用 Top 10% 收益衡量投资价值，两者并不等价。
3. 三专家下，50% 等权混合意味着每个专家最低约占 16.7%；长期低效专家仍持续参与融合。全员非正技能时回到等权，是原算法约定，不是赚钱依据。

v2 先只增加一种方法：以已成熟的校准收益预测损失，更新全局专家权重。可采用带等权先验正则的非负、和为 1 的权重拟合，或低参数的指数加权更新，二选一作为主方案。允许低效专家权重接近零，用正则控制权重集中和变化；不要立即同时优化多个可靠性指标。

保持四个对照：等权、仅训练期估计的静态权重、旧 Rank IC 门控、v2 损失门控。相同专家、相同校准和同一组合/执行模块下比较。数据扩大后，再研究成本后效用驱动的门控；此时专家虚拟组合必须经过相同执行规则，防止把不同仓位/换手收益归因给预测能力。

先学习 Agent × Time 权重。状态条件通过低维特征或向全局可靠性收缩实现；暂不学习每股票、每天、每专家的一套权重。21 个时间外窗口不足以支持四个状态各自独立估计复杂门控。

### D. 建立简洁的风险与成本优化器

第一个数学目标建议为：

`max_w mu^T w - lambda_risk * w^T Sigma w - estimated_cost(w - w_previous)`

其中 `mu` 与 `Sigma` 使用同一期限和收益单位；`w_previous` 是当前实际持仓漂移后的权重。约束包括多头、现金、单票上限和换手上限，流动性限制使用决策时可见的历史成交额。候选股票数必须与仓位上限、总投资比例共同可行。

协方差先用收缩估计或简单因子结构。成本先用明确的费用与滑点模型；真实最低佣金、交易数量取整等由执行账本结算，并报告优化估计成本与实际模拟成本之差。不给原版目标一次性塞入六个可调惩罚系数。

第二步单独把方差风险换为场景 CVaR，比较尾部损失、回撤、收益和换手。场景必须来自当时已可见历史，报告尾部样本支持度。只有有独立依据才增加收益估计不确定性项，避免重复计数风险。

现金作为正式可行资产/配置结果，按事先声明的收益假设记账。优化失败显式中止该次实验，不自动改成等权或忽略约束。

### E. 用信息差异改进专家

先保留三个 v1 专家完成上述比较。之后每轮只研究一个有经济解释的增量：趋势增加市场残差或波动调整；反转区分市场共同下跌与个股偏离；量价确认与真实流动性能力分离。当前量价公式是 alpha 分数，不能充当成交容量模型。

专家评价至少包括独立表现、误差相关、移除该专家后的组合变化。移除实验分开报告直接删除重归一化和训练期重新拟合，避免把重训效果混在专家贡献里。低相关不自动意味着高收益，也不因单次负 IC 立即翻转整个策略方向。

异构信息按基本面/公告、新闻、宏观的顺序推进。文本 Agent 的第一项可交付能力是从带时间戳原文抽取结构化事件、方向、影响期限与证据；收益映射仍交给历史校准。对比简单事件规则与 LLM 提取，才能识别 LLM 的增量。

五日是统一交易预测期限；慢变化的基本面可以作为条件特征，不能把季度估值观点直接当五日收益。无事件时表达“无事件证据”，不等于预计收益为零。历史 LLM 回放除文本时点外还需考虑模型训练记忆；后续前向留出对文本 Agent 尤其重要。

### F. 用可检验的实验替代功能堆叠

预先指定一个主判据，例如在同一风险/换手预算下的成本后超额收益；同时报告净值、最大回撤、风险调整表现、成本、换手与成交率。使用按时间块配对的比较与不确定区间，保留失败实验。

开发期滚动切分，校准和参数选择只发生在训练部分；训练标签跨越验证边界的样本应剔除。若改为每日发出五日预测，标签会重叠，不能把它们当独立五日样本。

2026-03 至 09 月已经被查看。本路线图依据这段失败结果提出 v2，因此该段对 v2 是诊断集，不再是最终未见测试。v1 结果保持原样；v2 在锁定版本之后使用新的未见时间段验证。需要更多开发市场状态时，再通过既有 BaoStock 路径补较早历史，不接触本地评分文件。

## 4. 后续四个里程碑

旧 `EXECUTION_PLAN.md` 的 P1–P12 作为阶段背景保留；以下按 `base.md` 的纵向闭环组织剩余工作，避免继续按目录层层铺开。

| 里程碑 | 具体交付 | 验收与决策 |
| --- | --- | --- |
| M1：完成现有 MAS 的交易闭环 | 当日固定组合；订单/成交/现金/持仓账本；五日成熟预测记录；原五种方法走同一执行流程 | 一次运行可追溯信号到权益；未来数据变化不能改变已形成的订单；现金与持仓能对账；公布毛收益、净收益、成本和未成交记录 |
| M2：校准 + 基础优化 + 门控比较 | score→收益映射；最小风险成本优化器；等权/静态/旧门控/v2 门控比较 | 固定组合构造比较门控，固定融合比较优化器；报告多个滚动区间的配对差异。没有门控增益就保留简单融合为主方案，动态门控仍作为研究分支 |
| M3：状态与尾部风险 | 少量可见市场状态；收缩的条件可靠性；独立 CVaR 对照 | 分开回答状态是否改善预测融合、CVaR 是否改善风险；给出各状态样本量及尾部样本支持度 |
| M4：异构文本 Agent 与挑战机制 | 首个基本面/公告专家；证据引用；触发式 Critic；必要时才接 LangGraph | 事件规则 vs LLM、无 Critic vs 有 Critic、无文本 vs 有文本；测净增益、校准、调用成本与延迟 |

News/Macro、语义记忆、复杂 Bull/Bear 多轮辩论和 FinRL 对照在 M4 之后按明确问题添加。Performance Memory 现在就随成熟预测账建设，文件/JSONL 足够。独立研究 Agent 先产出，再允许 Critic 看它们的证据；Critic 不回写当时原始预测，只产生可消融的挑战特征。

### M1 的首批实现范围

优先沿用新增的 `backtest/simulator.py` 和 `portfolio/allocation.py`，把市场时间推进、订单费用、现金持仓和当日组合固定这四个职责补齐。只有代码复杂度实际需要时再拆出 engine/execution/accounting；无需为了目录规划创建第二套实现。复用现有 agents/reliability，避免先搭建通用插件、消息总线和空类体系。

先保留 v1 的收盘后决策、次日开盘入场、第五日收盘计划退出协议；计划退出订单提前登记，不使用第五日收盘信号决定同一收盘价交易。日线成交是显式模型假设。模拟器每天推进，已停牌的持仓不会在五日窗口结束时凭空消失，期末未退出资产单独列示。

随后把“持续持仓、仅交易权重差额”作为独立协议比较。若每五个交易日都在次日开盘调仓，典型持有区间变成 t+1 开盘至 t+6 开盘，不能沿用 t+1 开盘至 t+5 收盘的标签而声称严格同期限。重新定义目标、成熟时间与费用后，让所有方法在同一协议下比较。

现有原始执行行情含 `tradestatus`、OHLC、成交额、`preclose`，但采集字段不含 `isST`。板块/风险警示、价格限制、申报数量和公司行动所需信息需按模拟规则补齐。不同股票不能统一猜成一个涨跌停比例或一套买入数量规则。只补执行确实需要的来源字段，不重新审计已有训练数据。

未复权价格用于订单，持仓记账仍须处理现金分红与拆并股等公司行动。若仅用后复权收益做总回报近似，必须明确它是近似账本，不能同时宣称已精确模拟原始股数和分红现金。

最有价值的测试：未来行情不能改写历史订单；标签到期才更新门控；停牌持仓延续；已知简单交易的费用与现金恒等；公司行动不虚增/吞掉权益；优化输出满足所声明约束。采用手算可验证的小案例与固定真实缓存重放，不围绕每个内部函数重复写校验测试。

## 5. 开源参考如何使用

截至本次核对，以下定位来自官方仓库、文档或论文。参考其方法和实验接口，按本项目契约独立实现；不搬运旧项目代码、特征或模型，不把多个完整框架接成相互依赖的主系统。

| 参考 | 可用价值 | 在本项目的落点 |
| --- | --- | --- |
| [Qlib 组合策略文档](https://qlib.readthedocs.io/en/latest/component/strategy.html) | 预测分数、组合策略与执行器之间的清晰接口 | M1/M2 优先参照职责边界与公平基线，不迁移整个数据框架 |
| [TradingAgents](https://github.com/TauricResearch/TradingAgents) | 专业分析、Bull/Bear 研究和图编排 | M4 的研究挑战/编排参考；本项目最终仓位由数学优化器决定 |
| [QuantHarness](https://github.com/Y-Research-SBU/QuantHarness) | 指标、形态和趋势分工，工具计算证据 | 数值计算留在工具侧；其高频/多市场定位不作为本项目五日 A 股有效性的证据 |
| [FinCon 官方仓库](https://github.com/The-FinAI/FinCon)、[论文](https://arxiv.org/abs/2407.06567) | Manager–Analyst 与反馈机制的研究启发 | 官方仓库目前只有 README，说明代码尚待公开；`base.md` 所列 `fincon/agents/...` 路径不能当作已经核实可用的官方源码 |
| [FinAgent 官方仓库](https://github.com/DVampire/FinAgent)、[论文](https://arxiv.org/abs/2402.18485) | 多模态信息、工具和记忆的研究参考 | M4 后评价是否需要额外信息与记忆；不先引入向量库 |
| [StockBench](https://github.com/ChenYXxxx/stockbench) | 连续多步决策、持仓和统一评测 | 参考评价协议；它的美股标的与供应商不是本项目当前数据方案 |
| [FinRL](https://github.com/AI4Finance-Foundation/FinRL) | 金融强化学习实验框架 | 闭环稳定后作同环境、同动作约束、同成本的可选基线 |

`base.md` 是目标设计，不是所有路径均已验证的源码目录。引用具体实现前固定仓库与版本并核对路径；名字相同的第三方复刻项目不替代论文官方代码。外部项目宣称的收益或训练污染控制不直接成为本项目的实验证据。

## 6. “拒绝防御性编程”的落实

- 外部 CSV/API/LLM 输出和配置只在入口验证一次；内部使用明确的数据契约，不逐层重复检查。
- 错误直接暴露，不 `except Exception` 后继续，不将失败预测填成 0，不将求解失败偷偷替换为等权。
- 缺失行情、停牌、现金不足、未知标签用明确业务状态表达，不用默认值掩盖。风险约束与延迟标签属于模型语义。
- 等权暖启动若保留，作为有名称、有参数、有实验对照的算法规则；不作为捕获任意异常后的通用 fallback。
- 只支持现阶段选定的数据源、研究协议和求解路径；需要第二个实现时再抽象接口。
- 不提前生成全套空目录、恢复服务、重试框架、数据库或多层 Agent 包装。
- 性能优化先测量。已看到研究过程反复扫描历史行、重建股票日期索引，可在确认耗时后预建索引和增量窗口；不为了潜在规模先做分布式改造。

M1 首版已经通过现有 BaoStock 缓存完成开发期与时间外诊断。下一项是 M2：在严格成熟的预测账上拟合分数到收益的折内映射，再分别固定组合比较融合门控、固定融合比较组合优化；保持同一执行/成本协议和日期块统计，不因已看过的 OOS 结果调参。当前这一阶段不需要额外数据集。
